import { About } from "../components/Navigation/about";

// test("should return text", () => {
//   render(<About />).should.contain(<h4>About</h4>);
// });

test("Should return true", () => {
  expect(true).toBe(true);
});
